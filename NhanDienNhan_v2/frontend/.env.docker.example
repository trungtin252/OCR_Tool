# Public backend URL included in the production frontend bundle.
VITE_BACKEND_URL=https://ocr.o2n.ai.vn
# Bound only to 127.0.0.1 for the cloudflared system service; never public.
OCR_TUNNEL_ORIGIN_PORT=3002
